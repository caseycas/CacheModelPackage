package org . nd4j . linalg . api . shape ; import com . google . common . primitives . Ints ; import lombok . AllArgsConstructor ; import lombok . Data ; import lombok . NoArgsConstructor ; public @Data @AllArgsConstructor @NoArgsConstructor class StridePermutation implements Comparable < StridePermutation > { private int permutation ; private int stride ; @Override public int compareTo ( StridePermutation o ) { int astride = this . stride , bstride = o . getStride ( ) ; if ( astride < <int> ) { astride = - astride ; } if ( bstride < <int> ) { bstride = - bstride ; } if ( astride = = bstride ) { int aperm = permutation , bperm = o . getPermutation ( ) ; return ( aperm < bperm ) ? - <int> : <int> ; } if ( astride > bstride ) { return - <int> ; } return <int> ; } public static StridePermutation [ ] create ( int [ ] stride ) { StridePermutation [ ] ret = new StridePermutation [ stride . length ] ; for ( int i = <int> ; i < stride . length ; i + + ) { ret [ i ] = new StridePermutation ( i , stride [ i ] ) ; } return ret ; } } 
