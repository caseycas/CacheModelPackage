package jcuda . runtime ; public class cudaExtent { public long width ; public long height ; public long depth ; public cudaExtent ( ) { } public cudaExtent ( int width , int height , int depth ) { this . width = width ; this . height = height ; this . depth = depth ; } public cudaExtent ( long width , long height , long depth ) { this . width = width ; this . height = height ; this . depth = depth ; } @Override public String toString ( ) { return <str> + <str> + width + <str> + <str> + height + <str> + <str> + depth + <str> ; } } 
