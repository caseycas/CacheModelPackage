package org . gradle . util . internal ; import com . google . common . collect . Lists ; import java . util . LinkedList ; import java . util . List ; public class LimitedDescription { private final LinkedList content ; private final int maxItems ; public LimitedDescription ( int maxItems ) { this . maxItems = maxItems ; this . content = new LinkedList ( ) ; } public LimitedDescription append ( String line ) { content . add ( <int> , line ) ; if ( content . size ( ) > maxItems ) { content . removeLast ( ) ; } return this ; } public String toString ( ) { if ( content . size ( ) = = <int> ) { return <str> ; } StringBuilder out = new StringBuilder ( ) ; List reversed = Lists . reverse ( content ) ; for ( Object item : reversed ) { out . append ( item ) . append ( <str> ) ; } return out . toString ( ) ; } } 
