package org . elasticsearch . cluster . routing ; import java . util . List ; public class PlainShardsIterator implements ShardsIterator { private final List < ShardRouting > shards ; private volatile int index ; public PlainShardsIterator ( List < ShardRouting > shards ) { this . shards = shards ; reset ( ) ; } @Override public void reset ( ) { index = <int> ; } @Override public int remaining ( ) { return shards . size ( ) - index ; } @Override public ShardRouting nextOrNull ( ) { if ( index = = shards . size ( ) ) { return null ; } else { return shards . get ( index + + ) ; } } @Override public int size ( ) { return shards . size ( ) ; } @Override public int sizeActive ( ) { int count = <int> ; for ( ShardRouting shard : shards ) { if ( shard . active ( ) ) { count + + ; } } return count ; } @Override public int assignedReplicasIncludingRelocating ( ) { int count = <int> ; for ( ShardRouting shard : shards ) { if ( shard . unassigned ( ) ) { continue ; } if ( shard . primary ( ) ) { if ( shard . relocating ( ) ) { count + + ; } } else { count + + ; if ( shard . relocating ( ) ) { count + + ; } } } return count ; } @Override public Iterable < ShardRouting > asUnordered ( ) { return shards ; } } 
