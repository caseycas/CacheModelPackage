package org . eclipse . debug . examples . core . midi . launcher ; import org . eclipse . core . runtime . IStatus ; public abstract class TimeControl extends SequencerControl { public TimeControl ( String name , MidiLaunch launch ) { super ( name , launch ) ; } @Override public String getValue ( ) { long position = getTimeValue ( ) ; int milli = ( int ) ( position & <hex> ) ; int sec = ( int ) ( position / <int> ) ; int min = sec / <int> ; sec = sec % <int> ; StringBuffer clock = new StringBuffer ( ) ; clock . append ( min ) ; while ( clock . length ( ) < <int> ) { clock . insert ( <int> , <int> ) ; } clock . append ( <str> ) ; clock . append ( sec ) ; while ( clock . length ( ) < <int> ) { clock . insert ( <int> , <int> ) ; } clock . append ( <str> ) ; clock . append ( milli ) ; while ( clock . length ( ) < <int> ) { clock . insert ( <int> , <int> ) ; } return clock . toString ( ) ; } protected abstract long getTimeValue ( ) ; @Override public boolean isEditable ( ) { return false ; } @Override public IStatus setValue ( String newValue ) { return null ; } @Override public IStatus validateValue ( String value ) { return null ; } } 
