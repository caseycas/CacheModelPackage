package packageName } ; import static org . junit . Assert . * ; public class $ { testClassName } { private final $ { productionClassName } production = new $ { productionClassName } ( <str> ) ; < % ( binding . hasVariable ( <str> ) ? testMethodCount : <int> ) . times { index - > % > @org.junit.Test public void test$ { index } ( ) { assertEquals ( production . getProperty ( ) , <str> ) ; } < % } % > } 
