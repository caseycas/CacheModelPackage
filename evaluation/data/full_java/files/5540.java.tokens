package org . elasticsearch . cluster . ack ; import org . elasticsearch . common . unit . TimeValue ; public abstract class ClusterStateUpdateRequest < T extends ClusterStateUpdateRequest < T > > implements AckedRequest { private TimeValue ackTimeout ; private TimeValue masterNodeTimeout ; @Override public TimeValue ackTimeout ( ) { return ackTimeout ; } @SuppressWarnings ( <str> ) public T ackTimeout ( TimeValue ackTimeout ) { this . ackTimeout = ackTimeout ; return ( T ) this ; } @Override public TimeValue masterNodeTimeout ( ) { return masterNodeTimeout ; } @SuppressWarnings ( <str> ) public T masterNodeTimeout ( TimeValue masterNodeTimeout ) { this . masterNodeTimeout = masterNodeTimeout ; return ( T ) this ; } } 
