package org . elasticsearch . common . inject . internal ; import org . elasticsearch . common . unit . TimeValue ; import java . util . logging . Logger ; public class Stopwatch { private static final Logger logger = Logger . getLogger ( Stopwatch . class . getName ( ) ) ; private long startNS = System . nanoTime ( ) ; public long reset ( ) { long nowNS = System . nanoTime ( ) ; try { return TimeValue . nsecToMSec ( nowNS - startNS ) ; } finally { startNS = nowNS ; } } public void resetAndLog ( String label ) { logger . fine ( label + <str> + reset ( ) + <str> ) ; } } 
