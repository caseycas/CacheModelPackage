package packageName } ; import org . testng . annotations . * ; import static org . testng . Assert . * ; public class $ { testClassName } { private final $ { productionClassName } production = new $ { productionClassName } ( <str> ) ; @Test public void testOne ( ) { for ( int i = <int> ; i < <int> ; i + + ) { System . out . println ( <str> + i ) ; System . err . println ( <str> + i ) ; } assertEquals ( production . getProperty ( ) , <str> ) ; } @Test public void testTwo ( ) { for ( int i = <int> ; i < <int> ; i + + ) { System . out . println ( <str> + i ) ; System . err . println ( <str> + i ) ; } assertEquals ( production . getProperty ( ) , <str> ) ; } } 
