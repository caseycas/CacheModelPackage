package org . elasticsearch . common . geo ; import org . apache . lucene . util . GeoHashUtils ; import org . elasticsearch . test . ESTestCase ; public class GeoHashTests extends ESTestCase { public void testGeohashAsLongRoutines ( ) { final GeoPoint expected = new GeoPoint ( ) ; final GeoPoint actual = new GeoPoint ( ) ; for ( double lat = - <int> ; lat < <int> ; lat + + ) { for ( double lng = - <int> ; lng < <int> ; lng + + ) { for ( int p = <int> ; p < = <int> ; p + + ) { long geoAsLong = GeoHashUtils . longEncode ( lng , lat , p ) ; String geohashFromLong = GeoHashUtils . stringEncode ( geoAsLong ) ; String geohash = GeoHashUtils . stringEncode ( lng , lat , p ) ; assertEquals ( geohash , geohashFromLong ) ; expected . resetFromGeoHash ( geohash ) ; actual . resetFromGeoHash ( geoAsLong ) ; assertEquals ( expected , actual ) ; } } } } } 
