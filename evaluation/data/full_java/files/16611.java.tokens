package org . junit . tests . experimental . rules ; import static org . junit . Assert . assertEquals ; import static org . junit . Assert . assertThat ; import static org . junit . experimental . results . PrintableResult . testResult ; import static org . junit . experimental . results . ResultMatchers . isSuccessful ; import org . junit . Rule ; import org . junit . Test ; import org . junit . rules . ExternalResource ; public class ExternalResourceRuleTest { private static String callSequence ; public static class UsesExternalResource { @Rule public ExternalResource resource = new ExternalResource ( ) { @Override protected void before ( ) throws Throwable { callSequence + = <str> ; } ; @Override protected void after ( ) { callSequence + = <str> ; } ; } ; @Test public void testFoo ( ) { callSequence + = <str> ; } } @Test public void externalResourceGeneratesCorrectSequence ( ) { callSequence = <str> ; assertThat ( testResult ( UsesExternalResource . class ) , isSuccessful ( ) ) ; assertEquals ( <str> , callSequence ) ; } } 
