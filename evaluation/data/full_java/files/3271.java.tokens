package jcuda . runtime ; public class cudaMemcpy3DParms { public cudaArray srcArray = new cudaArray ( ) ; public cudaPos srcPos = new cudaPos ( ) ; public cudaPitchedPtr srcPtr = new cudaPitchedPtr ( ) ; public cudaArray dstArray = new cudaArray ( ) ; public cudaPos dstPos = new cudaPos ( ) ; public cudaPitchedPtr dstPtr = new cudaPitchedPtr ( ) ; public cudaExtent extent = new cudaExtent ( ) ; public int kind ; @Override public String toString ( ) { return <str> + createString ( <str> ) + <str> ; } public String toFormattedString ( ) { return <str> + createString ( <str> ) ; } private String createString ( String f ) { return <str> + srcArray + f + <str> + srcPos + f + <str> + srcPtr + f + <str> + dstArray + f + <str> + dstPos + f + <str> + dstPtr + f + <str> + extent + f + <str> + cudaMemcpyKind . stringFor ( kind ) ; } } 
