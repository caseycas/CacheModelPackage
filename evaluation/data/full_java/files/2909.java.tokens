package jcuda . runtime ; import jcuda . Pointer ; public class cudaPointerAttributes { public int memoryType ; public int device ; public Pointer devicePointer = new Pointer ( ) ; public Pointer hostPointer = new Pointer ( ) ; public int isManaged ; public cudaPointerAttributes ( ) { } @Override public String toString ( ) { return <str> + <str> + cudaMemoryType . stringFor ( memoryType ) + <str> + <str> + device + <str> + <str> + devicePointer + <str> + <str> + hostPointer + <str> + <str> + isManaged + <str> ; } } ; 
