package org . elasticsearch . common . blobstore . support ; import org . elasticsearch . common . blobstore . BlobMetaData ; public class PlainBlobMetaData implements BlobMetaData { private final String name ; private final long length ; public PlainBlobMetaData ( String name , long length ) { this . name = name ; this . length = length ; } @Override public String name ( ) { return this . name ; } @Override public long length ( ) { return this . length ; } @Override public String toString ( ) { return <str> + name + <str> + length + <str> ; } } 
