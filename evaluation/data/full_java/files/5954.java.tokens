package org . elasticsearch . common . logging ; public class DeprecationLogger { private final ESLogger logger ; public DeprecationLogger ( ESLogger parentLogger ) { String name = parentLogger . getName ( ) ; if ( name . startsWith ( <str> ) ) { name = name . replace ( <str> , <str> ) ; } else { name = <str> + name ; } this . logger = ESLoggerFactory . getLogger ( parentLogger . getPrefix ( ) , name ) ; } public void deprecated ( String msg , Object . . . params ) { logger . debug ( msg , params ) ; } } 
