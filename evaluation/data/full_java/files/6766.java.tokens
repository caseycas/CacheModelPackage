package org . elasticsearch . index . similarity ; import org . apache . lucene . search . similarities . * ; import org . elasticsearch . common . settings . Settings ; public abstract class AbstractSimilarityProvider implements SimilarityProvider { protected static final Normalization NO_NORMALIZATION = new Normalization . NoNormalization ( ) ; private final String name ; protected AbstractSimilarityProvider ( String name ) { this . name = name ; } @Override public String name ( ) { return this . name ; } protected Normalization parseNormalization ( Settings settings ) { String normalization = settings . get ( <str> ) ; if ( <str> . equals ( normalization ) ) { return NO_NORMALIZATION ; } else if ( <str> . equals ( normalization ) ) { float c = settings . getAsFloat ( <str> , <float> ) ; return new NormalizationH1 ( c ) ; } else if ( <str> . equals ( normalization ) ) { float c = settings . getAsFloat ( <str> , <float> ) ; return new NormalizationH2 ( c ) ; } else if ( <str> . equals ( normalization ) ) { float c = settings . getAsFloat ( <str> , <int> f ) ; return new NormalizationH3 ( c ) ; } else if ( <str> . equals ( normalization ) ) { float z = settings . getAsFloat ( <str> , <float> ) ; return new NormalizationZ ( z ) ; } else { throw new IllegalArgumentException ( <str> + normalization + <str> ) ; } } } 
