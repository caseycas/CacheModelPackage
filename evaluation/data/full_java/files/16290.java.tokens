package org . gradle . foundation ; import junit . framework . TestCase ; import org . gradle . foundation . output . FileLink ; import org . gradle . foundation . output . FileLinkDefinitionLord ; import org . gradle . foundation . output . OutputParser ; import java . io . File ; import java . util . List ; public class FileLinkTests extends TestCase { public static void parseOutputTest ( String textToSearch , FileLink . . . expectedResults ) { parseTest ( textToSearch , false , expectedResults ) ; } public static void parseTest ( String textToSearch , boolean verifyFileExists , FileLink . . . expectedResults ) { OutputParser outputParser = new OutputParser ( new FileLinkDefinitionLord ( ) , verifyFileExists ) ; List < FileLink > fileLinks = outputParser . parseText ( textToSearch ) ; TestUtility . assertListContents ( fileLinks , expectedResults ) ; } public void testCompileErrors ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) ) ; } public void testNotes ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testGradleBuildFile ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testCheckstyleSingleErrorOutput ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) ) ; } public void testCheckstyleReportErrorFile ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testHTMLReport ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testOffByOneCharacterBug ( ) { String outputText = <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testFailedTestsReportFile ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testMultiples ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , - <int> ) ) ; } public void testFileExtensionCaseSensitivity ( ) { String outputText = <str> + <str> + <str> + <str> + <str> + <str> + <str> ; parseOutputTest ( outputText , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) , new FileLink ( new File ( <str> ) , <int> , <int> , <int> ) ) ; } } 
