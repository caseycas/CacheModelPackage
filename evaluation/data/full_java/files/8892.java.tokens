package org . elasticsearch . cloud . aws ; import com . amazonaws . ClientConfiguration ; import org . elasticsearch . test . ESTestCase ; import static org . hamcrest . CoreMatchers . is ; public class AWSSignersTests extends ESTestCase { public void testSigners ( ) { assertThat ( signerTester ( null ) , is ( false ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; assertThat ( signerTester ( <str> ) , is ( false ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; assertThat ( signerTester ( <str> ) , is ( true ) ) ; ClientConfiguration configuration = new ClientConfiguration ( ) ; AwsSigner . configureSigner ( <str> , configuration , <str> ) ; assertEquals ( configuration . getSignerOverride ( ) , <str> ) ; AwsSigner . configureSigner ( <str> , configuration , <str> ) ; assertEquals ( configuration . getSignerOverride ( ) , <str> ) ; } public void testV2InInvalidRegion ( ) { try { AwsSigner . validateSignerType ( <str> , <str> ) ; fail ( <str> ) ; } catch ( IllegalArgumentException e ) { assertEquals ( <str> , e . getMessage ( ) ) ; } try { AwsSigner . validateSignerType ( <str> , <str> ) ; fail ( <str> ) ; } catch ( IllegalArgumentException e ) { assertEquals ( <str> , e . getMessage ( ) ) ; } } private boolean signerTester ( String signer ) { try { AwsSigner . validateSignerType ( signer , <str> ) ; return true ; } catch ( IllegalArgumentException e ) { return false ; } } } 
