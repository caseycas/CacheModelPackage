package com . google . common . primitives ; import com . google . common . annotations . GwtCompatible ; import javax . annotation . CheckReturnValue ; @CheckReturnValue @GwtCompatible final class ParseRequest { final String rawValue ; final int radix ; private ParseRequest ( String rawValue , int radix ) { this . rawValue = rawValue ; this . radix = radix ; } static ParseRequest fromString ( String stringValue ) { if ( stringValue . length ( ) = = <int> ) { throw new NumberFormatException ( <str> ) ; } String rawValue ; int radix ; char firstChar = stringValue . charAt ( <int> ) ; if ( stringValue . startsWith ( <str> ) | | stringValue . startsWith ( <str> ) ) { rawValue = stringValue . substring ( <int> ) ; radix = <int> ; } else if ( firstChar = = <str> ) { rawValue = stringValue . substring ( <int> ) ; radix = <int> ; } else if ( firstChar = = <str> & & stringValue . length ( ) > <int> ) { rawValue = stringValue . substring ( <int> ) ; radix = <int> ; } else { rawValue = stringValue ; radix = <int> ; } return new ParseRequest ( rawValue , radix ) ; } } 
