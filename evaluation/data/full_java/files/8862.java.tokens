package org . elasticsearch . cloud . azure . blobstore ; import java . io . IOException ; import java . io . OutputStream ; public class AzureOutputStream extends OutputStream { private final OutputStream blobOutputStream ; public AzureOutputStream ( OutputStream blobOutputStream ) { this . blobOutputStream = blobOutputStream ; } @Override public void write ( int b ) throws IOException { blobOutputStream . write ( b ) ; } @Override public void close ( ) throws IOException { try { blobOutputStream . close ( ) ; } catch ( IOException e ) { } } } 
