package io . netty . resolver . dns ; import java . net . InetSocketAddress ; abstract class DefaultDnsServerAddresses extends DnsServerAddresses { protected final InetSocketAddress [ ] addresses ; private final String strVal ; DefaultDnsServerAddresses ( String type , InetSocketAddress [ ] addresses ) { this . addresses = addresses ; final StringBuilder buf = new StringBuilder ( type . length ( ) + <int> + addresses . length * <int> ) ; buf . append ( type ) . append ( <str> ) ; for ( InetSocketAddress a : addresses ) { buf . append ( a ) . append ( <str> ) ; } buf . setLength ( buf . length ( ) - <int> ) ; buf . append ( <str> ) ; strVal = buf . toString ( ) ; } @Override public String toString ( ) { return strVal ; } } 
