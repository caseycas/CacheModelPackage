package org . gradle . foundation ; public class PathParserPortion { private String firstPart ; private String remainder ; public PathParserPortion ( String path ) { if ( path ! = null & & path . length ( ) > <int> ) { if ( path . startsWith ( <str> ) ) { path = path . substring ( <int> ) ; } int indexOfColon = path . indexOf ( <str> ) ; if ( indexOfColon = = - <int> ) { firstPart = path ; } else { firstPart = path . substring ( <int> , indexOfColon ) ; remainder = path . substring ( indexOfColon + <int> ) ; } } } public String getFirstPart ( ) { return firstPart ; } public String getRemainder ( ) { return remainder ; } public boolean hasRemainder ( ) { return remainder ! = null ; } public String toString ( ) { return firstPart + <str> + remainder ; } } 
