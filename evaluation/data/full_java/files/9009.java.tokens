package org . elasticsearch . test . rest ; import java . util . regex . Pattern ; final class BlacklistedPathPatternMatcher { private final Pattern pattern ; BlacklistedPathPatternMatcher ( String p ) { if ( p = = null | | p . trim ( ) . isEmpty ( ) ) { throw new IllegalArgumentException ( <str> ) ; } String finalPattern = p . replaceAll ( <str> , <str> ) . replaceAll ( <str> , <str> ) ; pattern = Pattern . compile ( <str> + finalPattern ) ; } public boolean isSuffixMatch ( String path ) { return pattern . matcher ( path ) . matches ( ) ; } } 
