package com . badlogic . gdx . tests . utils ; import com . badlogic . gdx . files . FileHandle ; public class AssetsFileGenerator { public static void main ( String [ ] args ) { FileHandle file = new FileHandle ( args [ <int> ] ) ; StringBuffer list = new StringBuffer ( ) ; args [ <int> ] = args [ <int> ] . replace ( <str> , <str> ) ; if ( ! args [ <int> ] . endsWith ( <str> ) ) args [ <int> ] = args [ <int> ] + <str> ; traverse ( file , args [ <int> ] , list ) ; new FileHandle ( args [ <int> ] + <str> ) . writeString ( list . toString ( ) , false ) ; } private static final void traverse ( FileHandle directory , String base , StringBuffer list ) { if ( directory . name ( ) . equals ( <str> ) ) return ; String dirName = directory . toString ( ) . replace ( <str> , <str> ) . replace ( base , <str> ) + <str> ; System . out . println ( dirName ) ; for ( FileHandle file : directory . list ( ) ) { if ( file . isDirectory ( ) ) { traverse ( file , base , list ) ; } else { String fileName = file . toString ( ) . replace ( <str> , <str> ) . replace ( base , <str> ) ; if ( fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) ) { list . append ( <str> + fileName + <str> ) ; System . out . println ( fileName ) ; } else if ( fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) | | file . extension ( ) . equals ( <str> ) | | fileName . endsWith ( <str> ) ) { list . append ( <str> + fileName + <str> ) ; System . out . println ( fileName ) ; } else { if ( fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) | | fileName . endsWith ( <str> ) ) continue ; list . append ( <str> + fileName + <str> ) ; System . out . println ( fileName ) ; } } } } } 
