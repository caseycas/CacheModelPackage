package rx ; import static org . junit . Assert . assertEquals ; import java . util . ArrayList ; import java . util . List ; import org . junit . Test ; public class StartWithTests { @Test public void startWith1 ( ) { List < String > values = Observable . just ( <str> , <str> ) . startWith ( <str> ) . toList ( ) . toBlocking ( ) . single ( ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; } @Test public void startWithIterable ( ) { List < String > li = new ArrayList < String > ( ) ; li . add ( <str> ) ; li . add ( <str> ) ; List < String > values = Observable . just ( <str> , <str> ) . startWith ( li ) . toList ( ) . toBlocking ( ) . single ( ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; } @Test public void startWithObservable ( ) { List < String > li = new ArrayList < String > ( ) ; li . add ( <str> ) ; li . add ( <str> ) ; List < String > values = Observable . just ( <str> , <str> ) . startWith ( Observable . from ( li ) ) . toList ( ) . toBlocking ( ) . single ( ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; assertEquals ( <str> , values . get ( <int> ) ) ; } } 
