package jcuda . runtime ; public class cudaMemcpy3DPeerParms { public cudaArray srcArray = new cudaArray ( ) ; public cudaPos srcPos = new cudaPos ( ) ; public cudaPitchedPtr srcPtr = new cudaPitchedPtr ( ) ; public int srcDevice ; public cudaArray dstArray = new cudaArray ( ) ; public cudaPos dstPos = new cudaPos ( ) ; public cudaPitchedPtr dstPtr = new cudaPitchedPtr ( ) ; public int dstDevice ; public cudaExtent extent = new cudaExtent ( ) ; @Override public String toString ( ) { return <str> + createString ( <str> ) + <str> ; } public String toFormattedString ( ) { return <str> + createString ( <str> ) ; } private String createString ( String f ) { return <str> + srcArray + f + <str> + srcPos + f + <str> + srcPtr + f + <str> + srcDevice + f + <str> + dstArray + f + <str> + dstPos + f + <str> + dstPtr + f + <str> + dstDevice + f + <str> + extent ; } }