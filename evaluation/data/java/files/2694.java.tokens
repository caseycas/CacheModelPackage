package jcuda . jcufft ; public class cufftHandle { private int plan ; private int dim = <int> ; private int type ; private int sizeX = <int> ; private int sizeY = <int> ; private int sizeZ = <int> ; private int batchSize = <int> ; public String toString ( ) { if ( dim = = <int> ) { return <str> ; } String result = <str> + plan + <str> + dim + <str> + cufftType . stringFor ( type ) + <str> ; switch ( dim ) { case <int> : result + = <str> + sizeX + <str> + batchSize ; break ; case <int> : result + = <str> + sizeX + <str> + sizeY + <str> ; break ; case <int> : result + = <str> + sizeX + <str> + sizeY + <str> + sizeZ + <str> ; break ; } result + = <str> ; return result ; } void setBatchSize ( int batchSize ) { this . batchSize = batchSize ; } void setType ( int type ) { this . type = type ; } void setDimension ( int dim ) { this . dim = dim ; } void setSize ( int x , int y , int z ) { this . sizeX = x ; this . sizeY = y ; this . sizeZ = z ; } }