require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do Random . new . bytes ( <int> ) . should be_an_instance_of ( String ) end it <str> do Random . new . bytes ( <int> ) . length . should == <int> end it <str> do Random . new . bytes ( <int> ) . encoding . should == Encoding :: ASCII_8BIT end it <str> do Random . new ( <int> ) . bytes ( <int> ) . should == Random . new ( <int> ) . bytes ( <int> ) end it <str> do rnd = Random . new ( <int> ) rnd . bytes ( <int> ) . should == <str> rnd . bytes ( <int> ) rnd . bytes ( <int> ) . should == <str> end it <str> do rnd = Random . new ( bignum_value ** <int> ) rnd . bytes ( <int> ) . should == <str> rnd . bytes ( <int> ) rnd . bytes ( <int> ) . should == <str> end it <str> do Random . new . bytes ( <int> ) . should_not == Random . new . bytes ( <int> ) end end 
