require <str> require <str> require <str> require <str> require <str> if ARGV . length == <int> $stderr . puts <str> $stderr . puts <str> $stderr . puts <str> $stderr . puts <str> $stderr . puts <str> Process . exit ( <int> ) else locale = ARGV [ <int> ] end FileUtils . mkdir_p ( <str> ) data = { <str> locale <str> = > <str> locale <str> , <str> locale <str> = > <str> locale <str> , <str> locale <str> = > <str> locale <str> } data . each do | sourcefile , destfile | if File . exists? ( sourcefile ) source = YAML . load open ( sourcefile ) dest = open ( destfile , <str> ) dest . write source . to_xml dest . close puts <str> destfile <str> else $stderr . puts <str> sourcefile <str> end end 
