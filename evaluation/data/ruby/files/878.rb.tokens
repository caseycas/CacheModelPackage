class Arangodb < Formula desc <str> homepage <str> url <str> sha256 <str> head <str> , <str> = > <str> bottle do sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end depends_on <str> = > <str> depends_on <str> needs <str> fails_with <str> do build <int> cause <str> end def install ENV . libcxx args = <str> prefix <str> share <str> var <str> args << <str> if build . head? if ENV . compiler != <str> ENV . append <str> , <str> end system <str> , * args system <str> , <str> end def post_install ( var / <str> ) . mkpath ( var / <str> ) . mkpath system <str> sbin <str> + ( build . head? ? <str> : <str> ) , <str> , <str> , <str> end def caveats ; <<- <str> EOS <str> . undent <str> EOS end plist_options <str> = > <str> HOMEBREW_PREFIX <str> + ( build . head? ? <str> : <str> ) + <str> def plist ; <<- <str> EOS <str> . undent <str> EOS end end 
