if ARGV . first != <str> ARGV [ <int> ] = <str> else ARGV . shift unless ARGV . delete ( <str> ) customrc = ARGV . index { | x | x . include? ( <str> ) } railsrc = if customrc File . expand_path ( ARGV . delete_at ( customrc ) . gsub ( <str> , <str> ) ) else File . join ( File . expand_path ( <str> ) , <str> ) end if File . exist? ( railsrc ) extra_args_string = File . read ( railsrc ) extra_args = extra_args_string . split ( <str> ) . flat_map ( & <str> ) puts <str> extra_args . join ( <str> ) <str> railsrc <str> ARGV . insert ( <int> , * extra_args ) end end end require <str> require <str> Rails :: Generators :: PluginGenerator . start 
