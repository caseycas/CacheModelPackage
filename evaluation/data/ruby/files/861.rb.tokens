require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do <str> . reverse . should == <str> <str> . reverse . should == <str> <str> . reverse . should == <str> end it <str> do <str> . taint . reverse . tainted? . should == true <str> . taint . reverse . tainted? . should == true end with_feature <str> do it <str> do <str> . reverse . should == <str> end end end describe <str> do it <str> do a = <str> a . reverse! . should equal ( a ) a . should == <str> <str> . reverse! . should == <str> end it <str> do lambda { <str> . freeze . reverse! } . should raise_error ( RuntimeError ) lambda { <str> . freeze . reverse! } . should raise_error ( RuntimeError ) end it <str> do lambda { <str> . freeze . reverse! } . should raise_error ( RuntimeError ) end with_feature <str> do it <str> do str = <str> str . reverse! str . should == <str> end end end 
