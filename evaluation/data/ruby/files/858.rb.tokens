require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do s = <str> s . end_with? ( <str> ) . should be_true s . end_with? ( <str> ) . should be_true end it <str> do s = <str> s . end_with? ( <str> ) . should be_false end it <str> do s = <str> s . end_with? ( <str> ) . should be_true end it <str> do <str> . end_with? ( <str> , <str> , <str> , <str> ) . should be_true end it <str> do s = <str> find = mock ( <str> ) find . should_receive ( <str> ) . and_return ( <str> ) s . end_with? ( find ) . should be_true end it <str> do <str> . end_with? ( ) . should be_false lambda { <str> . end_with? ( <int> ) } . should raise_error ( TypeError ) lambda { <str> . end_with? ( [ <str> ] ) } . should raise_error ( TypeError ) lambda { <str> . end_with? ( <int> , nil , <str> ) } . should raise_error ( TypeError ) end it <str> do find = mock ( <str> ) find . should_not_receive ( <str> ) <str> . end_with? ( <str> , find ) . should be_true end it <str> do old_kcode = $KCODE begin $KCODE = <str> <str> . end_with? ( <str> ) . should be_true ensure $KCODE = old_kcode end end end 
