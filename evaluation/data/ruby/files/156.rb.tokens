require <str> require <str> describe Puppet :: Settings :: ValueTranslator do let ( <str> ) { Puppet :: Settings :: ValueTranslator . new } context <str> do it <str> do expect ( translator [ <str> ] ) . to eq ( true ) expect ( translator [ <str> ] ) . to eq ( false ) end it <str> do expect ( translator [ true ] ) . to eq ( true ) expect ( translator [ false ] ) . to eq ( false ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) expect ( translator [ <str> ] ) . to eq ( <str> ) expect ( translator [ <str> ] ) . to eq ( <str> ) expect ( translator [ <str> ] ) . to eq ( <str> ) end end context <str> do it <str> do expect ( translator [ <str> ] ) . to eq ( <int> ) expect ( translator [ <str> ] ) . to eq ( <int> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <int> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end end context <str> do it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end it <str> do expect ( translator [ <str> ] ) . to eq ( <str> ) end end end 
