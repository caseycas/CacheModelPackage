class Kettle < Formula desc <str> homepage <str> url <str> sha256 <str> bottle <str> def install rm_rf Dir [ <str> ] libexec . install Dir [ <str> ] ( etc + <str> ) . install libexec + <str> = > <str> ( etc + <str> ) . install libexec + <str> ( etc + <str> ) . mkpath ( var + <str> ) . mkpath <str> . each do | command | ( bin + <str> command <str> ) . write_env_script libexec + <str> command <str> , <str> = > libexec end end def plist ; <<- <str> EOS <str> . undent <str> EOS end test do ENV . java_cache system <str> bin <str> , <str> libexec <str> , <str> end end 
