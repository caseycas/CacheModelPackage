test_name <str> confine <str> , <str> = > <str> require <str> extend Puppet :: Acceptance :: SMFUtils teardown do step <str> agents . each do | agent | clean agent , <str> = > <str> end end agents . each do | agent | manifest , method = setup agent , <str> = > <str> step <str> apply_manifest_on ( agent , <str> % manifest ) do assert_match ( / ensure changed <str> . * to <str> / , result . stdout , <str> agent <str> ) end step <str> on agent , <str> do assert_match ( <str> , result . stdout , <str> agent <str> ) end end 
