require <str> require <str> module Bosh::Dev::Sandbox describe Postgresql do subject ( <str> ) { described_class . new ( <str> , logger , <int> , runner ) } let ( <str> ) { instance_double ( <str> ) } describe <str> do it <str> do expect ( runner ) . to receive ( <str> ) . with ( <str> ) postgresql . create_db end end describe <str> do it <str> do expect ( runner ) . to receive ( <str> ) . with ( <str> ) postgresql . drop_db end end describe <str> do it <str> do expect ( subject . db_name ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . username ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . password ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . adapter ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . port ) . to eq ( <int> ) end end end end 
