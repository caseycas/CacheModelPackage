require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do <str> . lstrip . should == <str> <str> . lstrip . should == <str> <str> . lstrip . should == <str> <str> . lstrip . should == <str> <str> . lstrip . should == <str> end not_compliant_on <str> do it <str> do <str> . lstrip . should == <str> end end it <str> do <str> . taint . lstrip . tainted? . should == true <str> . taint . lstrip . tainted? . should == true <str> . taint . lstrip . tainted? . should == true end end describe <str> do it <str> do a = <str> a . lstrip! . should equal ( a ) a . should == <str> a = <str> a . lstrip! a . should == <str> end it <str> do a = <str> a . lstrip! . should == nil a . should == <str> end it <str> do lambda { <str> . freeze . lstrip! } . should raise_error ( RuntimeError ) end it <str> do lambda { <str> . freeze . lstrip! } . should raise_error ( RuntimeError ) lambda { <str> . freeze . lstrip! } . should raise_error ( RuntimeError ) end end 
