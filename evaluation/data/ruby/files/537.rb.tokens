module Sortable extend ActiveSupport :: Concern included do default_scope { order_id_desc } scope <str> , - > { reorder ( id : <str> ) } scope <str> , - > { reorder ( id : <str> ) } scope <str> , - > { reorder ( <str> : <str> ) } scope <str> , - > { reorder ( <str> : <str> ) } scope <str> , - > { reorder ( <str> : <str> ) } scope <str> , - > { reorder ( <str> : <str> ) } scope <str> , - > { reorder ( name : <str> ) } scope <str> , - > { reorder ( name : <str> ) } end module ClassMethods def order_by ( method ) case method . to_s when <str> then order_name_asc when <str> then order_name_desc when <str> then order_updated_asc when <str> then order_updated_desc when <str> then order_created_asc when <str> then order_created_desc when <str> then order_id_desc when <str> then order_id_asc else all end end end end 
