require <str> require <str> require <str> module Msf module Payload::Php::SendUUID def php_send_uuid ( opts = { } ) sock_var = opts [ <str> ] || <str> sock_type = opts [ <str> ] || <str> uuid = opts [ <str> ] || generate_payload_uuid uuid_raw = uuid . to_raw . chars . map { | c | <str> % c . ord } . join ( <str> ) php = <str> uuid_raw <str> sock_type <str> sock_var <str> sock_var <str> php end end end 
