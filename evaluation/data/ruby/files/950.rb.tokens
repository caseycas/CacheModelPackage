require <str> require <str> require <str> module Bosh::Dev class ShipitLifecycle def initialize @shell = Bosh :: Core :: Shell . new end def pull @shell . run ( <str> current_branch <str> , <str> : true ) end def push raise <str> if current_branch == <str> @shell . run ( <str> current_branch <str> , <str> : true ) end private def current_branch current_repo_path = Rugged :: Repository . discover ( <str> ) current_repo = Rugged :: Repository . new ( current_repo_path ) current_repo . head . name . split ( <str> ) . last end end end 
