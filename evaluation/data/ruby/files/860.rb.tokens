require <str> describe AuthHelper do describe <str> do it <str> do allow ( helper ) . to receive ( <str> ) { [ <str> , <str> ] } expect ( helper . button_based_providers ) . to include ( * [ <str> , <str> ] ) end it <str> do allow ( helper ) . to receive ( <str> ) { [ <str> , <str> ] } expect ( helper . button_based_providers ) . to include ( <str> ) end it <str> do allow ( helper ) . to receive ( <str> ) { [ ] } expect ( helper . button_based_providers ) . to eq ( [ ] ) end end end 
