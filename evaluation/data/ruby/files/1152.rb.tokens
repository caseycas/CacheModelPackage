require <str> require <str> require <str> describe EqlMatcher do it <str> do EqlMatcher . new ( <int> ) . matches? ( <int> ) . should == true EqlMatcher . new ( <int> . <int> ) . matches? ( <int> . <int> ) . should == true EqlMatcher . new ( <str> ) . matches? ( <str> ) . should == true EqlMatcher . new ( <str> ) . matches? ( <str> ) . should == true EqlMatcher . new ( Object ) . matches? ( Object ) . should == true o = Object . new EqlMatcher . new ( o ) . matches? ( o ) . should == true end it <str> do EqlMatcher . new ( <int> ) . matches? ( <int> . <int> ) . should == false EqlMatcher . new ( Hash ) . matches? ( Object ) . should == false end it <str> do matcher = EqlMatcher . new ( <str> ) matcher . matches? ( <str> ) matcher . failure_message . should == [ <str> , <str> ] end it <str> do matcher = EqlMatcher . new ( <int> ) matcher . matches? ( <int> . <int> ) matcher . negative_failure_message . should == [ <str> , <str> ] end end 
