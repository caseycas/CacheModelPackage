require <str> class Wellington < Formula desc <str> homepage <str> stable do url <str> sha256 <str> end bottle do cellar <str> revision <int> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end devel do url <str> sha256 <str> version <str> end needs <str> head do url <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> end depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> go_resource <str> do url <str> , <str> = > <str> end go_resource <str> do url <str> , <str> = > <str> end def install ENV . cxx11 if MacOS . version < <str> version = File . read ( <str> ) . chomp ENV [ <str> ] = buildpath Language :: Go . stage_deps resources , buildpath / <str> system <str> , <str> if build . head? ENV . cxx11 ENV [ <str> ] = buildpath / <str> ENV . append <str> , <str> if ENV . compiler == <str> cd <str> do system <str> , <str> end end mkdir_p buildpath / <str> ln_s buildpath , buildpath / <str> system <str> , <str> , <str> , <str> version <str> , <str> , <str> , <str> bin . install <str> end test do s = <str> expected = <<- <str> EOS <str> . undent <str> EOS assert_equal expected , pipe_output ( <str> bin <str> , s , <int> ) end end 
