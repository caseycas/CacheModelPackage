class Itstool < Formula desc <str> homepage <str> url <str> sha256 <str> revision <int> bottle do cellar <str> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end head do url <str> depends_on <str> = > <str> depends_on <str> = > <str> end depends_on <str> if MacOS . version < = <str> depends_on <str> = > <str> def install ENV . append_path <str> , <str> Formula [ <str> ] . opt_lib <str> system <str> if build . head? system <str> , <str> libexec <str> system <str> , <str> bin . install Dir [ <str> libexec <str> ] bin . env_script_all_files ( libexec / <str> , <str> = > ENV [ <str> ] ) pkgshare . install_symlink libexec / <str> man1 . install_symlink libexec / <str> end test do ( testpath / <str> ) . write <<- <str> EOS <str> . undent <str> EOS system bin / <str> , <str> , <str> , <str> assert_match <str> , File . read ( <str> ) end end 
