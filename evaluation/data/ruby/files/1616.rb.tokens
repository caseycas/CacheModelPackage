require <str> require <str> require <str> describe BeTrueMatcher do it <str> do BeTrueMatcher . new . matches? ( true ) . should == true end it <str> do BeTrueMatcher . new . matches? ( <str> ) . should == false BeTrueMatcher . new . matches? ( false ) . should == false BeTrueMatcher . new . matches? ( nil ) . should == false BeTrueMatcher . new . matches? ( <int> ) . should == false end it <str> do matcher = BeTrueMatcher . new matcher . matches? ( <str> ) matcher . failure_message . should == [ <str> , <str> ] end it <str> do matcher = BeTrueMatcher . new matcher . matches? ( true ) matcher . negative_failure_message . should == [ <str> , <str> ] end end 
