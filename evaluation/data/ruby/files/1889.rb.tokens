require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> <str> . to_s . should == <str> <str> . to_s . should == <str> <str> . to_s . should == <str> end it <str> do <str> . to_s . should == <str> end end 
