require <str> describe ProjectTeam , <str> : true do describe <str> do let ( <str> ) { create ( <str> ) } let ( <str> ) { create ( <str> , <str> : group ) } let ( <str> ) { create ( <str> ) } before do project . team << [ user , <str> ] <int> . times do project . team << [ create ( <str> ) , <str> ] project . group . add_user ( create ( <str> ) , <str> ) end end benchmark_subject { project . team . max_member_access ( user . id ) } it { is_expected . to iterate_per_second ( <int> ) } end end 
