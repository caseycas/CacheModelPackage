describe <str> , <str> = > true do it <str> do <str> . unpack ( <str> + unpack_format ) . should be_an_instance_of ( Array ) end it <str> do d = mock ( <str> ) d . should_receive ( <str> ) . and_return ( <str> + unpack_format ) <str> . unpack ( d ) . should be_an_instance_of ( Array ) end it <str> do lambda { <str> . unpack ( nil ) } . should raise_error ( TypeError ) end it <str> do lambda { <str> . unpack ( <int> ) } . should raise_error ( TypeError ) end end describe <str> , <str> = > true do it <str> do lambda { <str> . unpack ( unpack_format ( <str> ) ) } . should raise_error ( ArgumentError ) end it <str> do lambda { <str> . unpack ( unpack_format ( <str> ) ) } . should raise_error ( ArgumentError ) end end 
