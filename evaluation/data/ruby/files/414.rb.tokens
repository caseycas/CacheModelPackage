require <str> require <str> describe Email do describe <str> do it <str> do expect ( Email . is_valid? ( nil ) ) . to eq ( false ) end it <str> do expect ( Email . is_valid? ( <str> ) ) . to eq ( true ) end it <str> do expect ( Email . is_valid? ( <str> ) ) . to eq ( false ) end it <str> do expect ( Email . is_valid? ( <str> ) ) . to eq ( true ) end it <str> do expect ( Email . is_valid? ( <str> ) ) . to eq ( false ) end end describe <str> do it <str> do expect ( Email . downcase ( <str> ) ) . to eq ( <str> ) expect ( Email . downcase ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( Email . downcase ( <str> ) ) . to eq ( <str> ) expect ( Email . downcase ( <str> ) ) . to eq ( <str> ) expect ( Email . downcase ( <str> ) ) . to eq ( <str> ) end end end 
