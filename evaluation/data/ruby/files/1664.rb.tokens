require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do <int> . lcm ( <int> ) . should == <int> <int> . lcm ( <int> ) . should == <int> end it <str> do <int> . lcm ( <int> ) . should be_kind_of ( Integer ) <int> . lcm ( <int> ) . should be_kind_of ( Integer ) end it <str> do <int> . lcm ( <int> ) . should == <int> <int> . lcm ( <int> ) . should == <int> end it <str> do - <int> . lcm ( <int> ) . should == <int> - <int> . lcm ( <int> ) . should == <int> end it <str> do <int> . lcm ( - <int> ) . should == <int> <int> . lcm ( - <int> ) . should == <int> end it <str> do - <int> . lcm ( - <int> ) . should == <int> - <int> . lcm ( - <int> ) . should == <int> end it <str> do bignum = <int> ** <int> bignum . should be_kind_of ( Bignum ) <int> . lcm ( bignum ) . should == bignum end it <str> do bignum = <int> ** <int> bignum . should be_kind_of ( Bignum ) bignum . lcm ( <int> ) . should == bignum end it <str> do lambda { <int> . lcm } . should raise_error ( ArgumentError ) end it <str> do lambda { <int> . lcm ( <int> , <int> ) } . should raise_error ( ArgumentError ) end it <str> do lambda { <int> . lcm ( <int> . <int> ) } . should raise_error ( TypeError ) lambda { <int> . lcm ( [ ] ) } . should raise_error ( TypeError ) end end 
