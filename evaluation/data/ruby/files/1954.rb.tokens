class Mg3a < Formula desc <str> homepage <str> url <str> sha256 <str> bottle do cellar <str> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end conflicts_with <str> , <str> = > <str> option <str> , <str> option <str> , <str> option <str> , <str> option <str> , <str> option <str> , <str> def install if build . with? ( <str> ) mg3aopts = <str> if build . with? ( <str> ) else mg3aopts = <str> mg3aopts << <str> if build . with? ( <str> ) mg3aopts << <str> if build . with? ( <str> ) || build . with? ( <str> ) mg3aopts << <str> if build . with? ( <str> ) || build . with? ( <str> ) end system <str> , <str> mg3aopts * <str> , <str> , <str> bin . install <str> doc . install Dir [ <str> ] doc . install Dir [ <str> ] end test do ( testpath / <str> ) . write <<- <str> EOS <str> . undent <str> EOS ( testpath / <str> ) . chmod <oct> system testpath / <str> end end 
