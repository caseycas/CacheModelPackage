class Cairo < Formula desc <str> homepage <str> url <str> mirror <str> sha256 <str> revision <int> bottle do sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end keg_only <str> option <str> depends_on <str> = > <str> depends_on <str> = > <str> if MacOS . version > <str> depends_on <str> depends_on <str> depends_on <str> depends_on <str> depends_on <str> def install ENV . universal_binary if build . universal? args = <str> prefix <str> if build . with? <str> args << <str> << <str> << <str> else args << <str> << <str> << <str> end system <str> , * args system <str> , <str> end test do ( testpath / <str> ) . write <<- <str> EOS <str> . undent <str> EOS fontconfig = Formula [ <str> ] freetype = Formula [ <str> ] gettext = Formula [ <str> ] glib = Formula [ <str> ] libpng = Formula [ <str> ] pixman = Formula [ <str> ] flags = ( ENV . cflags || <str> ) . split + ( ENV . cppflags || <str> ) . split + ( ENV . ldflags || <str> ) . split flags += <str> fontconfig . opt_include <str> freetype . opt_include <str> gettext . opt_include <str> glib . opt_include <str> glib . opt_lib <str> include <str> libpng . opt_include <str> pixman . opt_include <str> lib <str> system ENV . cc , <str> , <str> , <str> , * flags system <str> end end 
