require File . expand_path ( File . dirname ( __FILE__ ) + <str> ) describe OutgoingMessage :: Template :: IncomingMessageFollowup do describe <str> do it <str> do msg = <str> expect { subject . body } . to raise_error ( ArgumentError , msg ) end it <str> do expected = <str> expect ( subject . body ( <str> = > <str> ) ) . to eq ( expected ) end it <str> do opts = { <str> = > <str> , <str> = > <str> } expected = <str> expect ( subject . body ( opts ) ) . to eq ( expected ) end end describe <str> do it <str> do expect ( subject . salutation ( <str> = > <str> ) ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . letter ) . to eq ( <str> ) end it <str> do expect ( subject . letter ( <str> = > <str> ) ) . to eq ( <str> ) end end describe <str> do it <str> do expect ( subject . signoff ) . to eq ( <str> ) end end end 
