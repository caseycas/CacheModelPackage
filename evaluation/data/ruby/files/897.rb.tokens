class Pango < Formula desc <str> homepage <str> stable do url <str> sha256 <str> end bottle do revision <int> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end head do url <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> end option <str> depends_on <str> = > <str> depends_on <str> depends_on <str> depends_on <str> depends_on <str> depends_on <str> fails_with <str> do build <int> cause <str> end def install ENV . universal_binary if build . universal? args = <str> prefix <str> share <str> system <str> if build . head? system <str> , * args system <str> system <str> , <str> end test do system <str> bin <str> , <str> ( testpath / <str> ) . write <<- <str> EOS <str> . undent <str> EOS cairo = Formula [ <str> ] fontconfig = Formula [ <str> ] freetype = Formula [ <str> ] gettext = Formula [ <str> ] glib = Formula [ <str> ] libpng = Formula [ <str> ] pixman = Formula [ <str> ] flags = ( ENV . cflags || <str> ) . split + ( ENV . cppflags || <str> ) . split + ( ENV . ldflags || <str> ) . split flags += <str> cairo . opt_include <str> fontconfig . opt_include <str> freetype . opt_include <str> gettext . opt_include <str> glib . opt_include <str> glib . opt_lib <str> include <str> libpng . opt_include <str> pixman . opt_include <str> cairo . opt_lib <str> gettext . opt_lib <str> glib . opt_lib <str> lib <str> system ENV . cc , <str> , <str> , <str> , * flags system <str> end end 
