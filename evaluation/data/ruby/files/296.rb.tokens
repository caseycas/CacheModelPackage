class Bokken < Formula desc <str> homepage <str> url <str> sha256 <str> bottle do cellar <str> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end depends_on <str> depends_on <str> depends_on <str> depends_on <str> depends_on <str> resource <str> do url <str> sha256 <str> end resource <str> do url <str> sha256 <str> end def install resource ( <str> ) . stage do cd <str> do system <str> mkdir_p libexec / <str> ( libexec / <str> ) . install <str> ln_s <str> , libexec / <str> end end resource ( <str> ) . stage do ( libexec / <str> ) . install Dir [ <str> ] inreplace libexec / <str> , <str> , <str> end python_path = <str> libexec <str> libexec <str> ld_library_path = <str> libexec <str> ( libexec / <str> ) . install Dir [ <str> ] ( bin / <str> ) . write <<- <str> EOS <str> . undent <str> EOS end end 
