require File . expand_path ( <str> , __FILE__ ) with_feature <str> do describe <str> do it <str> do Encoding . locale_charmap . should be_an_instance_of ( String ) end platform_is <str> do it <str> do old_lc_all = ENV [ <str> ] ENV [ <str> ] = <str> ruby_exe ( <str> ) . should == <str> ENV [ <str> ] = old_lc_all end end platform_is <str> , <str> do it <str> do old_lc_all = ENV [ <str> ] ENV [ <str> ] = <str> ruby_exe ( <str> ) . should == <str> ENV [ <str> ] = old_lc_all end end platform_is <str> , <str> do it <str> do old_lc_all = ENV [ <str> ] ENV [ <str> ] = <str> ruby_exe ( <str> ) . should == <str> ENV [ <str> ] = old_lc_all end end platform_is <str> = > [ <str> , <str> , <str> ] do it <str> do old_charmap = Encoding . locale_charmap old_lc_all = ENV [ <str> ] ENV [ <str> ] = <str> Encoding . locale_charmap . should == old_charmap ENV [ <str> ] = old_lc_all end end end end 
