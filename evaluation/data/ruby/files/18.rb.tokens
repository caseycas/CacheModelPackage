require <str> describe InvitationCode , <str> = > <str> do it <str> do expect ( FactoryGirl . build ( <str> ) ) . to be_valid end it <str> do code = FactoryGirl . create ( <str> ) expect ( code . count ) . to be > <int> end describe <str> do it <str> do code = FactoryGirl . create ( <str> ) expect { code . use! } . to change ( code , <str> ) . by ( - <int> ) end end describe <str> do it <str> do AppConfig . admins . account = <str> expect ( InvitationCode . default_inviter_or ( alice ) . username ) . to eq ( <str> ) end it <str> do AppConfig . admins . account = <str> expect ( InvitationCode . default_inviter_or ( alice ) . username ) . to eq ( <str> ) end end end 
