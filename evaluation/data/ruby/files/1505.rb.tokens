module Puppet class Vendor class << self def vendor_dir File . join ( [ File . dirname ( File . expand_path ( __FILE__ ) ) , <str> ] ) end def load_entry ( entry ) Puppet . debug ( <str> $1 <str> ) load <str> vendor_dir <str> entry <str> end def require_libs require <str> end def load_vendored Dir . entries ( vendor_dir ) . each do | entry | if entry . match ( <str> ) load_entry entry end end require_libs end end end end 
