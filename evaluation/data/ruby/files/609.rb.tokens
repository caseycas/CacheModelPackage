require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) describe <str> do before <str> do DirSpecs . create_mock_dirs end after <str> do DirSpecs . delete_mock_dirs end before <str> do @dir = Dir . open DirSpecs . mock_dir end after <str> do @dir . close end it <str> do first = @dir . pos a = @dir . read b = @dir . read prejmp = @dir . pos ret = @dir . rewind second = @dir . pos c = @dir . read a . should_not == b b . should_not == c c . should == a second . should_not == prejmp end it <str> do @dir . rewind . should == @dir end it_behaves_like <str> , <str> end 
