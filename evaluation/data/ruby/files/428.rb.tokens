require File . expand_path ( File . dirname ( __FILE__ ) + <str> ) describe AboutMeValidator do describe <str> do it <str> do params = { <str> = > <str> } validator = AboutMeValidator . new ( params ) expect ( validator . about_me ) . to eq ( <str> ) end end describe <str> do it <str> do params = { <str> = > <str> * <int> } validator = AboutMeValidator . new ( params ) expect ( validator ) . to be_valid end it <str> do params = { <str> = > <str> } validator = AboutMeValidator . new ( params ) expect ( validator ) . to be_valid end it <str> do params = { <str> = > nil } validator = AboutMeValidator . new ( params ) expect ( validator ) . to be_valid end it <str> do params = { <str> = > <str> * <int> } validator = AboutMeValidator . new ( params ) validator . valid? expect ( validator . errors [ <str> ] . size ) . to eq ( <int> ) end end describe <str> do it <str> do params = { <str> = > <str> } validator = AboutMeValidator . new ( params ) expect ( validator . about_me ) . to eq ( <str> ) end end end 
