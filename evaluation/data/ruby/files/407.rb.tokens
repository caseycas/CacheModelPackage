require <str> describe RejectionMailer do describe <str> do context <str> do let ( <str> ) { Fabricate ( <str> ) } let ( <str> ) { { <str> : <str> , <str> : user . email , <str> : SiteSetting . title } } let ( <str> ) { RejectionMailer . send_rejection ( <str> , user . email , template_args ) } it <str> do expect ( reject_mail . to ) . to eql ( [ user . email ] ) end it <str> do expect ( reject_mail . subject ) . to be_present end it <str> do expect ( reject_mail . subject ) . to match ( SiteSetting . title ) end it <str> do expect ( reject_mail . body ) . to be_present end end end end 
