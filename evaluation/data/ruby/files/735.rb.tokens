require <str> require <str> puts <str> RUBY_VERSION <str> BUILD_CONFIG = Rubinius :: BUILD_CONFIG gems = BUILD_CONFIG [ <str> ] install_dir = <str> BUILD_CONFIG [ <str> ] <str> BUILD_CONFIG [ <str> ] <str> unless BUILD_CONFIG [ <str> ] and ENV [ <str> ] != <str> and <str> . chomp . size > <int> and $? . success? and ( openssl = <str> ENV [ <str> ] || <str> . chomp ) . size > <int> openssl = false end options = { <str> = > nil , <str> = > [ ] , <str> = > true , <str> = > false , <str> = > false , <str> = > true , <str> = > nil , <str> = > nil , <str> = > true , <str> = > false , <str> = > install_dir } def install ( gem , install_dir , options ) return if File . directory? <str> install_dir <str> gem [ <int> .. - <int> ] <str> file = File . join ( BUILD_CONFIG [ <str> ] , <str> gem <str> ) installer = Gem :: Installer . new file , options spec = installer . install puts <str> spec . name <str> spec . version <str> end gems . each do | gem | next if gem =~ <str> if gem =~ <str> and openssl options [ <str> ] = [ <str> openssl <str> openssl <str> ] else options [ <str> ] = [ ] end install gem , install_dir , options end if RUBY_PLATFORM =~ <str> install ( gems . find { | g | g =~ <str> } , install_dir , options ) else install ( gems . find { | g | g =~ <str> } , install_dir , options ) end 
