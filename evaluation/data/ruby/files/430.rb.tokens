require <str> require <str> module Bosh module Cli class BasicLoginStrategy def initialize ( terminal , director , config , interactive ) @terminal = terminal @director = director @config = config @interactive = interactive end def login ( target , username , password ) if @interactive username = @terminal . ask ( <str> ) if username . blank? password = @terminal . ask_password ( <str> ) if password . blank? end if username . blank? || password . blank? err ( <str> ) end if @director . login ( username , password ) @terminal . say_green ( <str> username <str> ) @config . set_credentials ( target , { <str> = > username , <str> = > password } ) @config . save else if @interactive @terminal . say_red ( <str> username <str> ) login ( target , username , <str> ) else err ( <str> username <str> ) end end end end end end 
