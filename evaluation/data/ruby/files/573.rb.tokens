require File . expand_path ( File . dirname ( __FILE__ ) + <str> ) describe Holiday do describe <str> do it <str> do holiday = Holiday . new expect ( holiday . valid? ) . to be false expect ( holiday . errors [ <str> ] ) . to eq ( [ <str> ] ) end end describe <str> do def due_date ( ymd ) return Holiday . due_date_from_working_days ( Date . strptime ( ymd ) , <int> ) . strftime ( <str> ) end context <str> do it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( due_date ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( Holiday . due_date_from_working_days ( Time . utc ( <int> , <oct> , <int> , <int> , <int> , <int> ) , <int> ) . strftime ( <str> ) ) . to eq ( <str> ) end end context <str> do it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Date . new ( <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end it <str> do expect ( Holiday . due_date_from_calendar_days ( Time . utc ( <int> , <oct> , <int> , <int> , <int> , <int> ) , <int> ) ) . to eq ( Date . new ( <int> , <int> , <int> ) ) end end end end 
