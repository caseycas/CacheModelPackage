module Bosh::Spec module AwsBootstrapCommandHelper def run ( cmd ) output = <str> IO . popen ( cmd ) . each do | line | puts line . chomp output << line end . close if $? . success? output else raise <str> cmd <str> Dir . pwd <str> $? . exitstatus <str> output <str> end end def run_bosh ( cmd , work_dir = nil ) Dir . chdir ( work_dir || ClientSandbox . bosh_work_dir ) do run <str> ClientSandbox . bosh_config <str> cmd <str> end end end end 
