require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do Class . should have_private_method ( <str> ) end it <str> do lambda { Fixnum . send <str> } . should raise_error ( TypeError ) lambda { Object . send <str> } . should raise_error ( TypeError ) end it <str> do lambda { BasicObject . send <str> } . should raise_error ( TypeError ) end describe <str> do before ( <str> ) do @uninitialized = Class . allocate end it <str> do lambda { @uninitialized . send ( <str> , Class ) } . should raise_error ( TypeError ) end end end 
