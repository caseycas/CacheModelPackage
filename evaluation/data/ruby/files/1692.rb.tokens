require <str> describe Workers :: ResetPassword do describe <str> do it <str> do expect { Workers :: ResetPassword . new . perform ( alice . id ) } . to change ( Devise . mailer . deliveries , <str> ) . by ( <int> ) end it <str> do Workers :: ResetPassword . new . perform ( alice . id ) mail = Devise . mailer . deliveries . last expect ( mail . to ) . to eq ( [ alice . email ] ) expect ( mail . body ) . to include ( <str> ) end end end 
