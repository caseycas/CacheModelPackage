require File . expand_path ( <str> , __FILE__ ) require File . expand_path ( <str> , __FILE__ ) describe <str> do it <str> do <str> . rstrip . should == <str> <str> . rstrip . should == <str> <str> . rstrip . should == <str> <str> . rstrip . should == <str> <str> . rstrip . should == <str> end it <str> do <str> . rstrip . should == <str> end it <str> do <str> . taint . rstrip . tainted? . should == true <str> . taint . rstrip . tainted? . should == true <str> . taint . rstrip . tainted? . should == true end end describe <str> do it <str> do a = <str> a . rstrip! . should equal ( a ) a . should == <str> end it <str> do a = <str> a . rstrip! a . should == <str> end it <str> do a = <str> a . rstrip! . should == nil a . should == <str> end it <str> do lambda { <str> . freeze . rstrip! } . should raise_error ( RuntimeError ) end it <str> do lambda { <str> . freeze . rstrip! } . should raise_error ( RuntimeError ) lambda { <str> . freeze . rstrip! } . should raise_error ( RuntimeError ) end end 
