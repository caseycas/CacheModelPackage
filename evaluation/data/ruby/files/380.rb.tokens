require <str> describe ApplicationHelper do describe <str> do it <str> do expect ( helper . escape_unicode ( <str> ) ) . to eq ( <str> ) end it <str> do expect ( helper . escape_unicode ( <str> ) ) . to match ( <str> ) end end describe <str> do context <str> do before do SiteSetting . stubs ( <str> ) . returns ( true ) end it <str> do session [ <str> ] = <str> expect ( helper . mobile_view? ) . to eq ( true ) end it <str> do session [ <str> ] = <str> expect ( helper . mobile_view? ) . to eq ( false ) end context <str> do it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to be_falsey end it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to eq ( true ) end it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to eq ( false ) end it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to be_falsey end it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to eq ( true ) end end end context <str> do before do SiteSetting . stubs ( <str> ) . returns ( false ) end it <str> do session [ <str> ] = <str> expect ( helper . mobile_view? ) . to eq ( false ) end it <str> do session [ <str> ] = <str> expect ( helper . mobile_view? ) . to eq ( false ) end context <str> do it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to eq ( false ) end it <str> do controller . request . stubs ( <str> ) . returns ( <str> ) expect ( helper . mobile_view? ) . to eq ( false ) end end end end describe <str> do it <str> do I18n . stubs ( <str> ) . returns ( <str> ) expect ( helper . rtl_class ) . to eq ( <str> ) end it <str> do I18n . stubs ( <str> ) . returns ( <str> ) expect ( helper . rtl_class ) . to eq ( <str> ) end end end 
