require File . expand_path ( <str> , __FILE__ ) describe <str> do describe <str> do it <str> do real = mock_numeric ( <str> ) real . should_receive ( <str> ) . and_return ( <str> ) Complex ( real , <int> ) . to_f . should == <str> end end describe <str> do it <str> do real = mock_numeric ( <str> ) real . should_receive ( <str> ) . and_return ( <str> ) Complex ( real , Rational ( <int> ) ) . to_f . should == <str> end end describe <str> do it <str> do real = mock_numeric ( <str> ) real . should_receive ( <str> ) . and_return ( <str> ) imag = mock_numeric ( <str> ) imag . should_receive ( <str> ) . with ( <int> ) . any_number_of_times . and_return ( true ) Complex ( real , imag ) . to_f . should == <str> end end describe <str> do it <str> do lambda { Complex ( <int> , <int> ) . to_f } . should raise_error ( RangeError ) end end describe <str> do it <str> do lambda { Complex ( <int> , <int> . <int> ) . to_f } . should raise_error ( RangeError ) end end end 
