require File . expand_path ( File . dirname ( __FILE__ ) + <str> ) require File . expand_path ( File . dirname ( __FILE__ ) + <str> ) describe RequestController , <str> do describe <str> do before ( <str> ) do load_raw_emails_data @dog_request = info_requests ( <str> ) ActionMailer :: Base . deliveries = [ ] end describe <str> do before <str> do @bob = login ( <str> ) end it <str> do using_session ( @bob ) do visit describe_state_message_path ( <str> = > @dog_request . url_title , <str> = > <str> ) fill_in <str> , <str> = > <str> click_button <str> expect ( page ) . to have_content <str> end deliveries = ActionMailer :: Base . deliveries expect ( deliveries . size ) . to eq ( <int> ) mail = deliveries [ <int> ] expect ( mail . body ) . to match ( <str> ) expect ( mail . body ) . to match ( <str> ) end end end end 
