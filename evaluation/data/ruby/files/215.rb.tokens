require <str> module Bosh::Director describe Canonicalizer do describe <str> do it <str> do expect ( Canonicalizer . canonicalize ( <str> ) ) . to eq ( <str> ) expect ( Canonicalizer . canonicalize ( <str> , <str> = > true ) ) . to eq ( <str> ) end it <str> do expect ( Canonicalizer . canonicalize ( <str> ) ) . to eq ( <str> ) expect ( Canonicalizer . canonicalize ( <str> , <str> = > true ) ) . to eq ( <str> ) end it <str> do expect ( Canonicalizer . canonicalize ( <str> ) ) . to eq ( <str> ) expect ( Canonicalizer . canonicalize ( <str> , <str> = > true ) ) . to eq ( <str> ) end it <str> do expect ( Canonicalizer . canonicalize ( <str> ) ) . to eq ( <str> ) expect ( Canonicalizer . canonicalize ( <str> , <str> = > true ) ) . to eq ( <str> ) end it <str> do expect { Canonicalizer . canonicalize ( <str> ) } . to raise_error ( DnsInvalidCanonicalName , <str> , ) expect { Canonicalizer . canonicalize ( <str> , <str> = > true ) } . to raise_error ( DnsInvalidCanonicalName , <str> , ) expect { Canonicalizer . canonicalize ( <str> ) } . to raise_error ( DnsInvalidCanonicalName , <str> , ) expect { Canonicalizer . canonicalize ( <str> , <str> = > true ) } . to raise_error ( DnsInvalidCanonicalName , <str> , ) end end end end 
