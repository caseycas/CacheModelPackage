class Links < Formula desc <str> homepage <str> url <str> mirror <str> sha256 <str> bottle do cellar <str> sha256 <str> = > <str> sha256 <str> = > <str> sha256 <str> = > <str> end depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> depends_on <str> = > <str> def install args = <str> prefix <str> man <str> if build . with? <str> args << <str> Formula [ <str> ] . opt_prefix <str> else args << <str> Formula [ <str> ] . opt_prefix <str> end args << <str> if build . with? <str> args << <str> if build . without? <str> args << <str> if build . without? <str> args << <str> if build . without? <str> system <str> , * args system <str> , <str> doc . install Dir [ <str> ] end test do system bin / <str> , <str> , <str> end end 
