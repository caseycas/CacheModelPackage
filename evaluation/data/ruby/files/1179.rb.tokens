require_relative <str> describe Settings , <str> : true do describe <str> do context <str> do it <str> do expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> end end context <str> do it <str> do expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> end context <str> do it <str> do expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> expect ( Settings . host_without_www ( <str> ) ) . to eq <str> end end end end end 
