require File . expand_path ( <str> , __FILE__ ) describe <str> , <str> = > true do before ( <str> ) do @numbers = [ <int> , Rational ( <int> , <int> ) , bignum_value , infinity_value ] end it <str> do @numbers . each do | number | number . send ( @method ) . should == <int> end end it <str> do @numbers . each do | number | ( <int> - number ) . send ( @method ) . should == Math :: PI end end describe <str> do it <str> do numeric = mock_numeric ( <str> ) numeric . should_receive ( <str> ) . with ( <int> ) . and_return ( false ) numeric . send ( @method ) . should == <int> end it <str> do numeric = mock_numeric ( <str> ) numeric . should_receive ( <str> ) . with ( <int> ) . and_return ( true ) numeric . send ( @method ) . should == Math :: PI end end end 
