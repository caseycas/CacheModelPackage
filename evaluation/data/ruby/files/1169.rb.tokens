require <str> describe Poll , <str> = > <str> do before do @poll = Poll . new ( <str> = > <str> ) end describe <str> do it <str> do @poll . poll_answers . build ( <str> = > <str> ) expect ( @poll ) . not_to be_valid end it <str> do @poll . poll_answers . build ( <str> = > <str> ) @poll . poll_answers . build ( <str> = > <str> ) expect ( @poll ) . to be_valid end it <str> do @poll . question = <str> @poll . valid? expect ( @poll . errors ) . to have_key ( <str> ) end end end 
